void foo() {
    for (int i = 0, 10) {
        foo();
        foo();
    }

    for (int i = 10, 0) foo();
    for (int i = 0, 10, -1) foo();

    for (int i = 0, 10)
        foo();

    for (int i = 10, 0, -1)
        foo();
}
