#include "../../civic.h"

export int main() {
    printInt(pow2(30));
    printNewlines(1);
    return 0;
}

int pow2(int a) {
    int x = 1;
    while ((bool) a) {
        x = x * 2;
        a = a - 1;
    }
    return x;
}