#include "../../civic.h"

int a = -1;
int foo() {
    a = a + 1;
    return a;
}

export int main() {
    int[2, 2] a = 0;

    a[foo()] = 1;

    for (int i = 0, 2) {
        for (int j = 0, 2) {
            printInt(a[i, j]);
            printSpaces(1);
        }
        printNewlines(1);
    }

    return 0;
}
